#include <iostream>
int main() {
int setpasswd = 2014; 
std::cout << "Enter password: ";
std::cin >> setpasswd;
if (setpasswd == 2014) {
    std::cout << "şifre doğru." << std::endl;
} else {
    std::cout << "şifre yanlış." << std::endl;
return 0;
}}
